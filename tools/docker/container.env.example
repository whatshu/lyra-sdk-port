# Written by tools/docker/build-image.sh; records what the release
# manifest attributes a build to.
SDK_IMAGE=shu-sdk:build-dev
SDK_IMAGE_SHA256=
SDK_TOOLCHAIN=apt
SDK_APT_PACKAGES=
SDK_TOOL_SHA256=
