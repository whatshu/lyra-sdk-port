name: uboot
description: Build U-Boot SPL + main image (MiniLoaderAll.bin / download.bin, uboot.img)
needs: []
run: run.sh
config:
  source: product/platform/configs/uboot
  dest: vendor/{vendor}/u-boot/configs
